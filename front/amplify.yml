version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - yarn global add nx
            - yarn install
        build:
          commands:
            - yarn build admin
      artifacts:
        baseDirectory: ../../dist/apps/todos
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: apps/admin
